[package]
name = "Jotphant"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A warm desktop Pomodoro task board with a Markdown notebook"
repository = "https://github.com/cosgunhalil/Jotphant"

[lib]
name = "jotphant"

[dependencies]
chrono = { version = "0.4.45", default-features = false, features = ["clock", "std"] }
directories = "6.0.0"
eframe = "0.35.0"
egui_commonmark = "0.24.0"
notify-rust = "4.18.0"
rusqlite = { version = "0.40.1", features = ["bundled"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_yaml_ng = "0.10.0"
sys-locale = "0.3.2"
thiserror = "2.0.19"
toml = "1.1.3"

# Optimize release binaries for size and a single distributable exe.
# Debug info is stripped but the symbol table is kept: fully-stripped binaries
# look "packed" to antivirus ML heuristics and draw false positives.
[profile.release]
codegen-units = 1
lto = true
strip = "debuginfo"

[lints.rust]
warnings = "deny"

[lints.clippy]
all = "deny"

[dev-dependencies]
chrono = { version = "0.4.45", default-features = false, features = ["clock", "std"] }
tempfile = "3.27.0"

[build-dependencies]
winresource = "0.1.31"
