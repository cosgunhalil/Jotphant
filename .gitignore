# Generated by Cargo / rustc — compiled artifacts and executables
/target/
debug/

# Cargo.lock is intentionally NOT ignored: Jotphant is a binary crate, so its
# lockfile is committed for reproducible builds.
# https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html

# rustfmt backup files
**/*.rs.bk

# MSVC debugging info
*.pdb

# IDE / editor
/.idea/

# Claude Code project instructions (local, not shared)
/CLAUDE.md
